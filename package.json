{
  "name": "gmaps-retag",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch run src/server.ts"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
